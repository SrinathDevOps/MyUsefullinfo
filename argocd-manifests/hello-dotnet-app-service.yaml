apiVersion: v1
kind: Service
metadata:
  name: hello-dotnet-app-service
spec:
  selector:
    app: hello-dotnet-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
